import { createSlice } from "@reduxjs/toolkit";

const MenusSlice = createSlice({
    name: 'menus',
    initialState: {food: true},
    reducers: {
        addMenu(state, action){
            
        },
        
    }
})

export default MenusSlice.reducer