import { createSlice } from '@reduxjs/toolkit'


const OsuTokenSlice = createSlice({
    name: "osutoken",
    initialState: "",
    reducers:{
        getToken(state, action){

        }
    }
})

export default OsuTokenSlice